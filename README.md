# رهنما سیستم شرق

سایت فروش نمایندگی رسمی سپیدار سیستم — مشهد، شهرک صنعتی توس.

## اجرای محلی

```bash
npm install
cp .env.example .env.local
```

سپس یک پستگرس محلی بالا بیاورید (داکر):

```bash
docker run -d --name rahnama-pg -e POSTGRES_USER=rahnama -e POSTGRES_PASSWORD=rahnama -e POSTGRES_DB=rahnama -p 5433:5432 postgres:16-alpine
```

ساختار دیتابیس و کاربر پنل:

```bash
npm run db:migrate
npm run db:create-admin -- admin "یک-رمز-قوی" "نام شما"
```

و بالاخره:

```bash
npm run dev
```

سایت روی `http://localhost:3000` و پنل روی `/admin/login`.

| دستور | کار |
| --- | --- |
| `npm run dev` | سرور توسعه |
| `npm run build` | ساخت نسخه production |
| `npm start` | اجرای نسخه ساخته‌شده |
| `npm run lint` | بررسی کد |
| `npm run typecheck` | بررسی تایپ‌ها |
| `npm run db:migrate` | ساخت/به‌روزرسانی جداول دیتابیس |
| `npm run db:create-admin` | ساخت کاربر پنل (یا تغییر رمزش) |

## ساختار پروژه

```
app/
  layout.tsx              فقط html/body/فونت — بدون هدر و فوتر
  icon.svg                فاوآیکون (نشان برند)
  not-found.tsx           صفحه ۴۰۴
  sitemap.ts              نقشه سایت (خودکار)
  robots.ts               robots.txt (خودکار)

  (site)/                 سایت عمومی — این پرانتز روی URL اثری ندارد
    layout.tsx            هدر + فوتر + schema سراسری
    page.tsx              صفحه اصلی
    products/             فهرست + صفحه هر محصول
    checkout/             تسویه حساب و بازگشت از درگاه
    consultation/         فرم مشاوره + تقویم رزرو تماس
    contact/              تماس با ما
    blog/                 فهرست، مقاله و دسته‌بندی‌ها
    [slug]/               لندینگ‌پیج صنفی (از دیتابیس)
    compare/              فهرست و صفحات مقایسه با رقبا

  admin/                  پنل مدیریت — پوسته جدا، بدون هدر سایت فروش
    login/                ورود
    prices/               ویرایش قیمت‌ها
    orders/               فهرست سفارش‌ها
    leads/                سرنخ‌ها (eCRM ساده)
    landing/              ساخت و ویرایش لندینگ‌پیج‌ها
    blog/                 نوشتن و انتشار مقاله
    settings/             وضعیت راه‌اندازی، رمز عبور، کاربران
    actions.ts            Server Actionها

components/
  site/                   اجزای مخصوص این سایت (هدر، فوتر، لوگو، کارت محصول)
  ui/                     اجزای عمومی (دکمه، سکشن، بردکرامب)

lib/
  site.ts                 اطلاعات کسب‌وکار: نام، آدرس، تلفن، ساعات کاری
  products.ts             کاتالوگ محصولات (ساختار ثابت)
  pricing.ts              قیمت و توضیحات از دیتابیس + باطل‌سازی کش
  nav.ts                  ساختار منو (از products.ts ساخته می‌شود)
  format.ts               اعداد فارسی، قیمت، تاریخ شمسی
  schema.ts               داده ساختاریافته schema.org
  db.ts                   اتصال پستگرس
  db/schema.sql           ساختار جداول
  auth.ts                 ورود پنل (scrypt + کوکی امضاشده)
  orders.ts               ساخت و پیگیری سفارش
  leads.ts                سرنخ‌ها و رزروها
  availability.ts         بازه‌های آزاد تماس (وقت تهران)
  mail.ts                 اعلان ایمیلی سرنخ‌ها
  landing.ts              خواندن/نوشتن لندینگ‌پیج‌ها + کش
  landing-content.ts      انواع داده و تبدیل متن ← ساختار (توابع خالص)
  blog.ts                 خواندن/نوشتن مقالات + کش
  blog-content.ts         دسته‌بندی‌ها، پارسر متن مقاله و لینک داخلی
  tracking.ts             رویدادها، وزن امتیاز و آستانه پاپ‌آپ
  health.ts               بررسی زنده اینکه چه چیزی تنظیم شده و چه چیزی نه
  payments/               درگاه‌ها پشت یک واسط مشترک
    types.ts              واسط PaymentProvider
    zarinpal.ts           پیاده‌سازی کامل
    snappay.ts            در انتظار مستندات پذیرندگی

scripts/
  migrate.mjs             اجرای schema.sql
  create-admin.mjs        ساخت کاربر پنل

_legacy/                  سایت قدیمی — فقط برای مرجع، در build شرکت نمی‌کند
```

## قواعد پروژه

**۱. افزودن محصول جدید = ویرایش یک فایل.**
یک شیء به آرایه‌ی `products` در `lib/products.ts` اضافه کنید. صفحه‌ی محصول، منو،
فوتر و `sitemap.xml` خودکار به‌روز می‌شوند.

**۲. تغییر آدرس یا تلفن = ویرایش `lib/site.ts`.**
همان مقدار در هدر، فوتر، صفحه تماس و `LocalBusiness` schema استفاده می‌شود.

**۳. هر صفحه دقیقاً یک `<h1>` دارد.**
عنوان اصلی صفحه با `<SectionHeading as="h1" />` نوشته می‌شود، بقیه سرتیترها `h2`.

**۴. راست‌چین با ویژگی‌های منطقی.**
به‌جای `left/right` از `start/end` استفاده کنید (`ps-4`, `me-2`, `start-0`).
`dir="rtl"` روی `<html>` است و این کلاس‌ها خودکار برعکس می‌شوند.

**۵. عدد فارسی برای محتوا، لاتین برای تماس.**
قیمت و آمار با `formatPrice()` / `formatNumber()`. شماره تلفن و ایمیل با
کلاس `ltr` تا در متن فارسی به‌هم نریزد.

**۶. هیچ درخواست خارجی.**
فونت وزیرمتن در زمان build دانلود و کنار سایت میزبانی می‌شود. تصاویر با
`next/image`. هیچ CDN خارجی اضافه نکنید — هم کند است هم برای کاربر ایرانی
غیرقابل‌اتکا.

## محیط‌ها

| محیط | شاخه گیت | ایندکس گوگل |
| --- | --- | --- |
| توسعه (محلی) | `dev` | ❌ `NEXT_PUBLIC_ALLOW_INDEXING=false` |
| production | `master` | ✅ `NEXT_PUBLIC_ALLOW_INDEXING=true` |

`robots.ts` تا وقتی `NEXT_PUBLIC_ALLOW_INDEXING=true` نباشد کل سایت را
`Disallow: /` می‌کند. این جلوی ایندکس‌شدن نسخه آزمایشی را می‌گیرد.

## دیپلوی روی لیارا

```bash
npx liara deploy
```

تنظیمات در `liara.json`. متغیرهای محیطی production از پنل لیارا ست می‌شوند
(نه از فایل `.env`).

## وضعیت فازها

- [x] **فاز ۰** — stack، ساختار، صفحه اصلی، ناوبری، sitemap
- [x] **فاز ۱** — کاتالوگ محصول، پنل قیمت، تسویه، زرین‌پال (اسنپ‌پی منتظر مستندات)
- [x] **فاز ۲** — واتساپ، فرم مشاوره، رزرو زمان تماس
- [x] **فاز ۳** — لندینگ‌پیج صنفی، صفحات مقایسه
- [x] **فاز ۴** — وبلاگ با دسته‌بندی و لینک داخلی اجباری
- [x] **فاز ۵** — GTM، امتیازدهی رفتاری، پاپ‌آپ، سرنخ نیمه‌کاره
- [x] **فاز ۶** — داشبورد، صفحه تنظیمات و وضعیت راه‌اندازی، مدیریت کاربر

## قواعد فاز ۱

**قیمت هرگز از فرم خوانده نمی‌شود.**
در `app/(site)/checkout/actions.ts` مبلغ همیشه سمت سرور از دیتابیس خوانده می‌شود.
اگر از فرم می‌آمد، کاربر می‌توانست در ابزار توسعه‌دهنده مرورگر عوضش کند.

**تومان در سایت، ریال در درگاه.**
همه‌جای سایت تومان است. تبدیل به ریال (×۱۰) فقط در `lib/payments/zarinpal.ts`
و `lib/schema.ts` انجام می‌شود. جای دیگری تکرارش نکنید.

**کلید «خرید آنلاین» فقط وقتی روشن شود که درگاه واقعاً کار کند.**
صفحه محصول کش می‌شود و وضعیت درگاه را نمی‌خواند؛ فقط این کلید را می‌بیند.
صفحه تسویه به‌عنوان تور ایمنی نبودِ درگاه را مدیریت می‌کند.

**افزودن درگاه سوم = یک فایل جدید در `lib/payments/`.**
واسط `PaymentProvider` را پیاده کنید و در `index.ts` ثبتش کنید. صفحه تسویه
و جریان سفارش دست نمی‌خورند.

## قواعد فاز ۲

**سرنخ هرگز به‌خاطر ایمیل از دست نمی‌رود.**
`app/(site)/consultation/actions.ts` اول در دیتابیس ذخیره می‌کند، بعد ایمیل
می‌فرستد. اگر SMTP خراب باشد فرم همچنان موفق است و ستون `notified` نشان
می‌دهد کدام اعلان‌ها نرفته‌اند.

**ساعت کاری به وقت تهران است، نه وقت سرور.**
سرور لیارا احتمالاً UTC است. `lib/availability.ts` تبدیل را با اختلاف ثابت
+۰۳:۳۰ انجام می‌دهد (ایران از ۱۴۰۱ ساعت تابستانی ندارد). اگر این تبدیل
نبود، رزرو ساعت ۱۰ صبح در پنل ۶:۳۰ صبح دیده می‌شد.

**زمان انتخابی سمت سرور دوباره اعتبارسنجی می‌شود.**
مقدار فرم قابل دستکاری است. `isValidSlot()` جمعه، ساعات غیرکاری، زمان‌های
گذشته و زمان‌های وسط بازه را رد می‌کند.

**دو رزرو همزمان با ایندکس یکتای دیتابیس گرفته می‌شود.**
`leads_slot_unique` یک ایندکس جزئی است؛ اگر دو نفر هم‌زمان یک بازه را
بفرستند، دومی خطای ۲۳۵۰۵ می‌گیرد و پیام فارسی می‌بیند.

**در چیدمان، `min-w-0` را فراموش نکنید.**
آیتم‌های grid و flex به‌طور پیش‌فرض زیر عرض محتوایشان جمع نمی‌شوند. نوار
افقی انتخاب روز بدون `min-w-0` کل صفحه موبایل را ۹۸۱ پیکسل عریض می‌کرد.

## قواعد فاز ۳

**افزودن لندینگ‌پیج جدید = پر کردن یک فرم، نه کدنویسی.**
آدرس `/admin/landing/new` را باز کنید، فرم را پر کنید و تیک «منتشر شود»
بزنید. صفحه، sitemap و لینک‌های داخلی همان لحظه ساخته می‌شوند.

**بلوک‌های تکرارشونده با متن ساده وارد می‌شوند، نه JSON.**
بخش‌ها با `### عنوان` جدا می‌شوند؛ سؤالات و ردیف‌های مقایسه هر خط یک
رکورد با جداکننده `|`. تبدیل در `lib/landing-content.ts` انجام می‌شود.

**پایان خط را همیشه یکسان کنید.**
مرورگر متن textarea را با CRLF می‌فرستد. تابع `splitLines()` این را حل
می‌کند. اگر پارسر جدیدی نوشتید حتماً از همان استفاده کنید — بدون آن،
الگوهایی که به انتهای خط تکیه دارند بی‌صدا شکست می‌خورند و محتوا دور
ریخته می‌شود. این باگ یک بار واقعاً اتفاق افتاد.

**اسلاگ صفحه منتشرشده را عوض نکنید.**
لینک‌های گوگل می‌شکنند. برای بازنشستگی یک صفحه، تیک انتشار را بردارید؛
حذف فقط وقتی که هرگز منتشر نشده باشد.

**مسیرهای ثابت سایت در `RESERVED_SLUGS` هستند.**
اگر مسیر جدیدی به سایت اضافه کردید (مثلاً `/faq`)، اسلاگش را به آن
مجموعه اضافه کنید تا کسی نتواند لندینگی با همان نام بسازد و مسیر واقعی
را پنهان کند.

## قواعد فاز ۴

**هر مقاله باید حداقل به یک صفحه محصول لینک بدهد.**
این الزام در `app/admin/blog/actions.ts` اجرا می‌شود و فرم بدون آن ذخیره
نمی‌شود. لینک داخلی مهم‌ترین سیگنال سئویی است که وبلاگ به صفحات فروش
منتقل می‌کند.

**لینک داخل متن بهتر از کارت پایین صفحه است.**
در متن مقاله بنویسید `[[manufacturing|نسخه تولیدی]]` تا همان‌جا لینک شود.
این محصول خودکار به فهرست «محصولات مرتبط» هم اضافه می‌شود. اگر اسلاگ
اشتباه باشد، متن ساده نمایش داده می‌شود نه لینک شکسته — و پنل هشدار
می‌دهد.

**متن مقاله هرگز به‌صورت HTML خام رندر نمی‌شود.**
`lib/blog-content.ts` متن را به بلوک‌های تایپ‌شده تبدیل می‌کند و
`ArticleBody` فقط همان‌ها را رندر می‌کند. نویسنده حتی به‌اشتباه هم
نمی‌تواند اسکریپت تزریق کند.

**فیلدهای فرم پنل باید کنترل‌شده باشند.**
React نسخه ۱۹ فرمی را که prop به نام `action` دارد بعد از هر ارسال پاک
می‌کند — حتی وقتی اکشن خطا برگردانده. با `defaultValue`، یک خطای
اعتبارسنجی کل مقاله‌ای که نوشته‌اید را از بین می‌برد. همه فرم‌های پنل
با `useState` نوشته شده‌اند.

**دسته‌بندی‌ها در کد تعریف می‌شوند.**
`CATEGORIES` در `lib/blog-content.ts`. افزودن دسته جدید یک شیء در آن
آرایه است و صفحه‌اش خودکار ساخته می‌شود.

## قواعد فاز ۵

**امتیازدهی به گوگل تگ منیجر وابسته نیست.**
منطق در `components/site/BehaviorTracker.tsx` اجرا می‌شود و رویدادها فقط
به `dataLayer` هم push می‌شوند تا GTM (اگر لود شد) بخواندشان. دلیلش این
است که اسکریپت گوگل در ایران گاهی کند یا مسدود است؛ اگر امتیازدهی رویش
سوار بود، برای بخشی از بازدیدکننده‌ها هیچ‌وقت کار نمی‌کرد و بدتر اینکه
کسی متوجه نمی‌شد.

**GTM اختیاری است.**
بدون `NEXT_PUBLIC_GTM_ID` هیچ اسکریپتی لود نمی‌شود. با آن، اسکریپت با
`afterInteractive` بعد از تعاملی‌شدن صفحه می‌آید و رندر اولیه را بلاک
نمی‌کند.

**وزن‌های امتیاز در یک فایل‌اند.**
`lib/tracking.ts`. اگر آستانه پاپ‌آپ آزاردهنده بود، `POPUP_THRESHOLD` را
بالا ببرید — جای دیگری دست نزنید.

**پاپ‌آپ عمداً محدود شده.**
یک‌بار در ۲۴ ساعت، هرگز در صفحه تسویه (کاربر وسط پرداخت است) و هرگز در
پنل. با Escape هم بسته می‌شود.

**ثبت فرم نیمه‌کاره به کاربر اطلاع داده شده.**
`app/api/lead/route.ts` داده‌ای را ذخیره می‌کند که کاربر آگاهانه ارسال
نکرده. برای همین در پایین فرم مشاوره صریحاً نوشته شده که ممکن است برای
پیگیری تماس بگیریم. **این جمله را حذف نکنید** — بدون آن، ذخیره‌کردن
شماره‌ای که کاربر عمداً نفرستاده برایش غافلگیرکننده است.

**مسیر `/api/lead` عمومی است، پس محدودیت نرخ دارد.**
پنج درخواست در دقیقه به‌ازای هر IP، در حافظه. اگر روزی سایت روی چند
نمونه اجرا شد، این باید به دیتابیس منتقل شود.

**`localStorage` همیشه در try/catch.**
در حالت ناشناس یا با تنظیمات سخت‌گیرانه مرورگر خطا می‌دهد. سایت باید
بدون آن هم کار کند — فقط بدون امتیازدهی.

## قواعد فاز ۶

**چک‌لیست راه‌اندازی کد است، نه مستندات.**
`lib/health.ts` واقعاً نگاه می‌کند چه چیزی تنظیم شده — دامنه، اینماد،
درگاه، SMTP، قیمت‌ها، محتوا. صفحه `/admin/settings` همان را نشان می‌دهد.
یادداشت در مستندات فراموش می‌شود؛ هشدار قرمز بالای داشبورد نه.

**هر مورد می‌گوید کارِ کیست.**
`owner: "you"` یعنی کار اداری یا حساب کاربری که فقط صاحب کسب‌وکار
می‌تواند انجام دهد. `owner: "dev"` یعنی کار فنی.

**موارد `blocking` یعنی سایت آماده انتشار نیست.**
دامنه، اجازه ایندکس، دیتابیس و وجود کاربر پنل. بقیه موارد سایت را
بهتر می‌کنند ولی مانع نیستند.

**تغییر رمز، رمز فعلی را هم می‌خواهد.**
اگر کسی پشت سیستمِ باز بنشیند، نتواند حساب را از دست صاحبش خارج کند.

**سیستم نقش نداریم و عمداً نداریم.**
هر کاربر پنل دسترسی یکسان دارد. برای تیم دو نفره، نقش‌بندی پیچیدگی
بی‌فایده است. اگر روزی تیم بزرگ شد، اینجا باید بازنگری شود.

**داشبورد با «چه کاری امروز» شروع می‌شود، نه با عدد.**
تماس‌های رزروشده و سرنخ‌های داغ بالای صفحه‌اند. کسی که صبح پنل را باز
می‌کند باید در سه ثانیه بداند با چه کسی تماس بگیرد.
